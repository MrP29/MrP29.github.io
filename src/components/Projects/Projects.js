import React from "react";

import classes from "./Projects.module.css";

const Projects = (props) => {
  return (
    <div className={classes.projects} name="projects">
      <h2>My Projects</h2>
    </div>
  );
};

export default Projects;
